class Solution {
public:
    int strStr(string haystack, string needle) {
        int sz = 0;
        int result = 0;
//         while(sz = haystack.find(needle, sz) != ) {
            
//         }
        return haystack.find(needle);
    }
};